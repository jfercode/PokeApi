# =====================================================
# PokeAPI Project - Variables de Entorno
# =====================================================
# IMPORTANTE: 
# 1. Copia este archivo como .env: cp .env.example .env
# 2. Reemplaza todos los valores CHANGE_THIS con tus credenciales reales
# 3. NUNCA hagas commit del archivo .env al repositorio
# =====================================================

# -----------------------------------------------------
# CONFIGURACIÓN DEL FRONTEND
# -----------------------------------------------------
# URL del backend API (usada por Vite en el frontend)
VITE_API_URL=http://localhost:3000/api

# URL del frontend (para redirecciones OAuth)
VITE_FRONTEND_URL=http://localhost:5173


# -----------------------------------------------------
# CONFIGURACIÓN DEL BACKEND
# -----------------------------------------------------
# Puerto donde correrá el servidor Node.js
PORT=3000

# Entorno de ejecución (development, production, test)
NODE_ENV=development


# -----------------------------------------------------
# CONFIGURACIÓN DE OAuth2
# -----------------------------------------------------
# Proveedor OAuth2 (google, github, etc.)
OAUTH_PROVIDER=google

# Client ID de tu aplicación OAuth2
# Google: https://console.cloud.google.com/apis/credentials
# GitHub: https://github.com/settings/developers
OAUTH_CLIENT_ID=CHANGE_THIS_YOUR_OAUTH_CLIENT_ID

# Client Secret de tu aplicación OAuth2
OAUTH_CLIENT_SECRET=CHANGE_THIS_YOUR_OAUTH_CLIENT_SECRET

# URL de callback después de la autenticación
OAUTH_CALLBACK_URL=http://localhost:3000/api/auth/callback

# Scopes requeridos (separados por coma)
# Google: profile,email
# GitHub: user:email
OAUTH_SCOPES=profile,email


# -----------------------------------------------------
# CONFIGURACIÓN DE JWT (JSON Web Tokens)
# -----------------------------------------------------
# Secret para firmar los JWT tokens
# Genera uno seguro con: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MINIMUM_32_CHARS

# Tiempo de expiración de los tokens
# Formato: 7d (días), 24h (horas), 60m (minutos)
JWT_EXPIRES_IN=7d

# Algoritmo de encriptación (HS256, RS256, etc.)
JWT_ALGORITHM=HS256


# -----------------------------------------------------
# CONFIGURACIÓN DE GENERACIÓN DE IMÁGENES
# -----------------------------------------------------
# API para generar imágenes de Pokémon

# Opción 1: Pollinations.ai (NO requiere API key)
IMAGE_API_PROVIDER=pollinations
IMAGE_API_URL=https://image.pollinations.ai/prompt
IMAGE_API_KEY=not_required

# Opción 2: Replicate (Requiere API key)
# Descomenta si usas Replicate:
# IMAGE_API_PROVIDER=replicate
# IMAGE_API_URL=https://api.replicate.com/v1/predictions
# IMAGE_API_KEY=CHANGE_THIS_YOUR_REPLICATE_API_KEY
# Obtén tu API key en: https://replicate.com/account/api-tokens

# Opción 3: Stability AI (Requiere API key)
# Descomenta si usas Stability AI:
# IMAGE_API_PROVIDER=stability
# IMAGE_API_URL=https://api.stability.ai/v1/generation/stable-diffusion-xl-1024-v1-0/text-to-image
# IMAGE_API_KEY=CHANGE_THIS_YOUR_STABILITY_API_KEY
# Obtén tu API key en: https://platform.stability.ai/account/keys


# -----------------------------------------------------
# CONFIGURACIÓN DE CORS
# -----------------------------------------------------
# Orígenes permitidos para CORS (separados por coma)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Permitir credenciales (cookies, headers de autenticación)
CORS_CREDENTIALS=true


# -----------------------------------------------------
# CONFIGURACIÓN DE SESIONES
# -----------------------------------------------------
# Secret para encriptar las sesiones
# Genera uno seguro con: openssl rand -base64 32
SESSION_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_FOR_SESSIONS

# Tiempo de expiración de la sesión (en milisegundos)
# 86400000 = 24 horas
SESSION_MAX_AGE=86400000

# Nombre de la cookie de sesión
SESSION_COOKIE_NAME=pokeapi_session


# -----------------------------------------------------
# CONFIGURACIÓN DE BASE DE DATOS (Opcional)
# -----------------------------------------------------
# Descomenta si necesitas una base de datos para funcionalidades futuras

# PostgreSQL
# DATABASE_URL=postgresql://user:password@localhost:5432/pokeapi_db

# MongoDB
# MONGODB_URI=mongodb://localhost:27017/pokeapi_db

# Redis (para caché o sesiones)
# REDIS_URL=redis://localhost:6379


# -----------------------------------------------------
# CONFIGURACIÓN DE LOGGING
# -----------------------------------------------------
# Nivel de logs (error, warn, info, debug)
LOG_LEVEL=info

# Formato de logs (json, pretty)
LOG_FORMAT=pretty


# -----------------------------------------------------
# CONFIGURACIÓN DE RATE LIMITING
# -----------------------------------------------------
# Número máximo de peticiones por ventana de tiempo
RATE_LIMIT_MAX=100

# Ventana de tiempo en minutos
RATE_LIMIT_WINDOW_MS=15


# -----------------------------------------------------
# CONFIGURACIÓN DE POKEAPI (Opcional)
# -----------------------------------------------------
# URL base de PokeAPI si necesitas consumir datos de Pokémon reales
POKEAPI_BASE_URL=https://pokeapi.co/api/v2

# Cache TTL para datos de PokeAPI (en segundos)
POKEAPI_CACHE_TTL=3600


# -----------------------------------------------------
# VARIABLES DE DESARROLLO (Solo para development)
# -----------------------------------------------------
# Habilitar modo debug
DEBUG=false

# Habilitar hot reload en el backend
HOT_RELOAD=true

# Mostrar stack traces completos en errores
SHOW_STACK_TRACE=true


# =====================================================
# GUÍA RÁPIDA DE CONFIGURACIÓN
# =====================================================
#
# 1. OAUTH2 CON GOOGLE:
#    - Ve a: https://console.cloud.google.com/apis/credentials
#    - Crea un proyecto nuevo o usa uno existente
#    - Habilita la API de Google+ 
#    - Crea credenciales OAuth 2.0
#    - Agrega como URI de redirección: http://localhost:3000/api/auth/callback
#    - Copia el Client ID y Client Secret aquí
#
# 2. OAUTH2 CON GITHUB:
#    - Ve a: https://github.com/settings/developers
#    - New OAuth App
#    - Homepage URL: http://localhost:5173
#    - Callback URL: http://localhost:3000/api/auth/callback
#    - Copia el Client ID y genera un Client Secret
#
# 3. GENERAR SECRETS SEGUROS:
#    - JWT_SECRET: openssl rand -base64 32
#    - SESSION_SECRET: openssl rand -base64 32
#
# 4. POLLINATIONS.AI (Recomendado - sin API key):
#    - No requiere configuración adicional
#    - Simplemente usa: IMAGE_API_PROVIDER=pollinations
#
# 5. REPLICATE (Alternativa con más control):
#    - Regístrate en: https://replicate.com
#    - Ve a: https://replicate.com/account/api-tokens
#    - Crea un nuevo token
#    - Copia el token en IMAGE_API_KEY
#
# =====================================================