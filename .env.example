# =====================================================
# PokeAPI Project - Variables de Entorno
# =====================================================
# IMPORTANTE: 
# 1. Copia este archivo como .env en la carpeta app/
# 2. NUNCA hagas commit del archivo .env al repositorio
# =====================================================

# Frontend Configuration
VITE_FRONTEND_URL=http://localhost:5173
VITE_BACKEND_URL=http://localhost:3000

# APIs Externas
VITE_POKEAPI_BASE=https://pokeapi.co/api/v2
VITE_POLLINATIONS_API=https://image.pollinations.ai/prompt

# localStorage Keys
VITE_STORAGE_KEY_FUSIONS=pokemonFusions

# OAuth2
VITE_OAUTH_CLIENT_ID=your_google_client_id_here
VITE_OAUTH_CALLBACK=http://localhost:3000/api/auth/callback

# Backend Configuration (Llenar cuando hagas backend)
PORT=3000
NODE_ENV=development

# JWT (Generar con: openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d